version: '3.8'

services:
  nav-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8081
